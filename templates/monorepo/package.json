{
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:backend": "bun --filter=elysia-boilerplate run dev",
    "dev:frontend": "bun --filter=frontend run dev",
    "build:backend": "bun --filter=elysia-boilerplate run build",
    "build:frontend": "bun --filter=frontend run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10"
  }
}
